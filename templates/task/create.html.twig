{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <div class="row">
        <div class="col">
        </div>
        <div class="col">
            {{ action == "creating" ?
                form(form, {"action": path("task_create"), "method": "POST"}) :
                form(form, {"action": path("task_edit", {"id": id}), "method": "POST"})
            }}
        </div>
        <div class="col">
        </div>
    </div>
</div>
{% endblock %}
